import { BuscadorProductosPipe } from './buscador-productos.pipe';

describe('BuscadorProductosPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscadorProductosPipe();
    expect(pipe).toBeTruthy();
  });
});
